<h1>Itens</h1>

<table>
  <tr>
    <th>Quantidade</th>
    <th>Pedido</th>
    <th>Cardapio</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @itempedidos.each do |itempedido| %>
  <tr>
    <td><%= itempedido.quantidade %></td>
    <td><%= itempedido.pedido.data %></td>
    <td><%= itempedido.cardapio.nome %></td>
    <td><%= link_to 'Show', itempedido %></td>
    <td><%= link_to 'Edit', (edit_itempedido_path itempedido) %></td>
    <td><%= link_to 'Excluir', itempedido, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Itempedido', new_itempedido_path %>
